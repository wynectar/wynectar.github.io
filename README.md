# 基于 Vite6 + Vue3.5 + Pinia + Ts + Naive UI + Vitepress

[vite](https://cn.vitejs.dev/config/)
[vue](https://cn.vuejs.org/)
[ts](http://tslang.cn/docs/home.html)
[pinia](https://pinia.vuejs.org/zh/core-concepts/)
[naive ui](https://www.naiveui.com/zh-CN/os-theme/components/button)
[marked](https://marked.js.org/)

## 项目相关

- 根据命令生成目录树，自定义标注每个文件或文件夹的作用
- 只记录主要目录，如果有特殊文件或文件夹，请在目录树下面单独标注
- 向上引用，

### 目录树

```
`项目名称`
├── src
│   ├── api  # 请求接口
│   ├── assets  # 静态资源，一般存放图片、音视频、css样式
│   │   └── vue.svg
│   ├── components  # 通用业务组件或元组件，组件中可能含有api
│   │   └── HelloWorld.vue
│   ├── directives # 指令集
│   ├── filters # 过滤器，一般用来数据转换
│   ├── router # 路由配置
│   ├── storage  # 前端存储数据，localStorage和sessionStorage的存取都在此文件夹
│   ├── store  # 状态管理中心
│   ├── types  # TypeScript 项目中的类型声明文件
│   ├── utils  # 工具库，自定义函数
│   ├── views  # 页面模版，路由页面或其他展示页面
│   ├── App.vue  # 试图入口
│   ├── main.ts  # 入口文件
│   ├── style.css  # 全局样式
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
└── yarn.lock
```

### 启动命令

```bash
# web启动 & 打包
npm i
npm run dev
npm run build

# vitepress启动 & 打包
npm run docs:dev
npm run docs:build
```

- 如果新增vitepress文档需打包后，web页面才能查看
- 如果web页面打包之前想把vitepress打包，需要使用组合命令，自己可自定义

## Git 提交信息规范（Conventional Commits）

### 常用类型及 emoji

| emoji | 类型       | 说明                                          |
| ----- | ---------- | --------------------------------------------- |
| 🚀    | `feat`     | **新功能** - 添加新功能                       |
| 🐛    | `fix`      | **修复** - 修复 bug                           |
| ⬆️    | `update`   | **更新** - 更新代码                           |
| 📚    | `docs`     | **文档** - 更新文档                           |
| 🎨    | `style`    | **样式** - 代码格式化、样式调整（不影响功能） |
| ♻️    | `refactor` | **重构** - 代码重构，不修复 bug 也不添加功能  |
| ⚡️   | `perf`     | **性能** - 性能优化改进                       |
| 🧪    | `test`     | **测试** - 添加或修改测试                     |
| 📦    | `build`    | **构建** - 构建系统或外部依赖变更             |
| 👷    | `ci`       | **持续集成** - CI 配置或脚本变更              |
| 🔧    | `chore`    | **杂项** - 其他不影响代码的变更               |
| ⏪    | `revert`   | **回滚** - 撤销之前的提交                     |

### 命令行提交

```bash
# 使用类型前缀
git commit -m "feat: 添加用户登录功能"

# 自动增加emoji
git commit -m "feat🚀: 添加用户登录功能"
```
